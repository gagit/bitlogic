{% extends 'base.html.twig' %}

{% block title %}Editar Contacto Cliente{% endblock %}

{% block body %}

<h5 class="card-header bg-secondary text-light" ><i class="fa fa-edit"></i>
    Editar Contacto </h5>
<hr/>
    {{ include('contact_client/_form.html.twig') }}
<hr/>
<ul>
    <li style="display: inline-flex">
        <a href="{{ path('app_client_edit', {'id': contact_client.id}) }}" class="btn btn-outline-info btn-back" data-toggle="tooltip" data-placement="top"
           title="Volver"
        ><i class="fa fa-arrow-left"></i></a>
    </li>
    <li style="display: inline-flex">
        {{ include('contact_client/_delete_form.html.twig') }}
    </li>
</ul>

{% endblock %}
