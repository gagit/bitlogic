{% extends 'base.html.twig' %}

{% block title %}Tipos de Identificación{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}

{% block body %}
<h5 class="card-header bg-secondary text-light" ><i class="fa fa-list"></i>Tipos de Identificación </h5>
<hr/>
{{ include('identification_type/_formFilter.html.twig') }}
<hr/>
    <a class="btn btn-outline-dark" href="{{ path('app_identification_type_new') }}"><i class="fa fa-plus"></i> </a>
<hr/>
<div class="navigation">
    {{ knp_pagination_render(identification_types) }}
</div>
<hr/>
    <table class="table table-striped">
        <thead>
            <tr>
{#                <th>Id</th>#}
                <th>Tipo</th>
                <th>Descripcion</th>
                <th>Activo</th>
                <th>Grupo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for identification_type in identification_types %}
            <tr>
{#                <td>{{ identification_type.id }}</td>#}
                <td>{{ identification_type.name }}</td>
                <td>{{ identification_type.description }}</td>
                <td>{{ identification_type.enabled ? '<i class="fa fa-check text-success"></i>' : '<i class="fa fa-times text-danger"></i>' }}</td>
                <td>{{ identification_type.typeIdentification }}</td>
                <td>
                    <ul>
                        <li style="display: inline-flex" >
                            <button type="button" class="btn btn-outline-dark btn-show"
                                    data-toggle="modal" data-target="#showDetail"
                                    data-source="{{ path('app_identification_type_show', {'id': identification_type.id}) }}">
                                <i class="fa fa-eye"></i>
                            </button>
                        </li>
                        <li style="display: inline-flex" >
                            <a class="btn btn-outline-dark" href="{{ path('app_identification_type_edit', {'id': identification_type.id}) }}">
                                <i class="fa fa-edit"></i> </a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <hr/>
    <div class="navigation">
        {{ knp_pagination_render(identification_types) }}
    </div>
    <div class="modal" id="showDetail" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-scrollable"  role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">

                </div>
            </div>
        </div>
    </div>
{% endblock %}
