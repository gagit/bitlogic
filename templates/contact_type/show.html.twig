<h5 class="card-header bg-secondary text-light" ><i class="fa fa-eye"></i>
    Ver Tipo de Contacto </h5>
<hr/>
    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact_type.id }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ contact_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ contact_type.description }}</td>
            </tr>
            <tr>
                <th>ContactGroup</th>
                <td>{{ contact_type.contactGroup }}</td>
            </tr>
            <tr>
                <th>FontAwesomeIcon</th>
                <td>{{ contact_type.fontAwesomeIcon }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ contact_type.enabled ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
<hr/>
<ul>
    <li style="display: inline-flex">
        <a href="{{ path('app_contact_type_index') }}" class="btn btn-outline-info btn-back" data-toggle="tooltip" data-placement="top"
           title="Volver"
        ><i class="fa fa-arrow-left"></i></a>
    </li>
    <li style="display: inline-flex">
        <a href="{{ path('app_contact_type_edit', {'id': contact_type.id}) }}" class="btn btn-outline-info btn-back" data-toggle="tooltip" data-placement="top"
           title="Editar"
        ><i class="fa fa-edit"></i></a>
    </li>
    <li style="display: inline-flex">
        {{ include('contact_type/_delete_form.html.twig') }}
    </li>
</ul>
