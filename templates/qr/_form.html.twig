{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="row">
    <div class="col-12 text-center m-0">
        <button class="btn btn-outline-dark"><i class="fa fa-qrcode"></i></button>
    </div>
</div>
{{ form_end(form) }}
