{% extends 'base.html.twig' %}

{% block title %}Editar Clientesnew{% endblock %}

{% block body %}

<h5 class="card-header bg-secondary text-light" ><i class="fa fa-edit"></i>
    Editar Clientesnew index </h5>
<hr/>
    {{ include('clientesnew/_form.html.twig') }}

<hr/>
<ul>
    <li style="display: inline-flex">
        <a href="{{ path('app_clientesnew_index') }}" class="btn btn-outline-info btn-back" data-toggle="tooltip" data-placement="top"
           title="Volver"
        ><i class="fa fa-arrow-left"></i></a>
    </li>
    <li style="display: inline-flex">
        {{ include('clientesnew/_delete_form.html.twig') }}
    </li>
</ul>

{% endblock %}
